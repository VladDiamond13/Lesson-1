fun main() {
    val word = "Hello World, i am glad to see you. How are you?" - тип даних в скобках текст 
    val separatedWords = word.split(" ") - тип даних, типу розділенні слова дорівнює ??? (тут не розумію, типу діляться??)
    var result = "" - тип даних результат дорівнює те що на виході в скобках
    repeat(separatedWords.size) - функція повторювання (тут не розумію, розділення слів по розміру??) 
    {
        repeat(separatedWords[it].length) повтор (далі не зрозумів??)
        {
                n->
            if ( n % 2 == 0) - якщо парна літера
            {
                result += separatedWords[it][n] - результат плюс або дорівнює значенню 

            }
            if (separatedWords[it].length - 1 == n)  якщо літера непарна
            {
                result += " " - результат разом парні і непарні
            }
        }
    }
    println(result) - видача результата
}

PS: я зрозумів з цього коду, що ми беремо набір слів (неважливо якої кількості слів), ділимо слова на літери і виводимо їх на парні і не парні, парні входять в результат
який ми на виході отримуємо,непарні мінусуються в результаті видачі. Соррі але поки я це так розумію) + дуже погано розумію як цю всю логіку переносити в код і правильно його
будувати щоб він працював
